# 可供外部审阅的问题包（未发送）

2026-09-23。当前仍能通过本地记录提出并检验具体机制，外部帮助不是继续工作的前置条件。本文准备给可能的车辆控制/状态估计审阅者；没有联系任何人。

## 需要判断的具体问题

三条完整路线、四个预先选定转弯窗口，同一 Lincoln MKZ、6/8 m/s、5 Hz 重接参考轨迹和20 Hz执行。CTE（横向跟踪误差）偏大与频繁转向动作同时存在。希望区分：目标点几何采样的离散纹波、参考轨迹对估计位姿重接引入的变化、后轴运动传播模型偏差，以及车辆响应。这里没有真实TCP模型推理，也没有背景交通；不能将其直接视为榜单表现。

## 已能复查的证据

| 检验 | 结果 | 不能据此推断 |
|---|---|---|
| 原始控制器逐值重放 | 六例2761帧与全部原diagnostics完全一致 | 任意新控制输出下运动还相同 |
| 同状态只跳过一次轨迹更新 | 5 Hz更新直接贡献瞬时raw steer跳变 | 降低规划频率一定改善驾驶 |
| 无噪声圆轨迹Hermite插值 | 显著降低解析几何纹波 | 实际带重接参考也必然改善 |
| 历史同状态Hermite shadow | 四个窗口raw rate P95均增加 | 新闭环实际emitted rate一定增加 |
| 后轴传播残差单系数检验 | baseline拟合、short留出都降低约89% | 识别了轮胎物理参数或跨车通用 |
| 固定系数完整定位重放 | 急右弯和两S改善，左弯退化 | 仅凭定位即可判驾驶收益 |
| 固定k真实六例闭环 | 四窗CTE下降，但156项仍有1项右弯航向失败 | 候选已合格或可改默认 |

数字、符号、样本和限制分别见[精确重放](diagnostics/README.md)、[传播审查](agents/pose-propagation-audit.md)、[单系数拟合](agents/pose-lateral-regression.md)、[定位重放](agents/pose-compensation-replay.md)。完整证据使用独立原始目录，后续失败不覆盖。

## 若本地试验仍不能判别，最有价值的帮助

请审查经验项 `v_right=-k*v²*world_gyro` 对后轴参考点的意义，以及如何设计最小的速度/曲率/车型留出试验，区分轮胎侧滑、刚体参考点差异和离散积分误差。现有日志有相邻位置、速率、角速度和后轴几何，但没有直接的完整3D速度向量；速度残差来自真实相邻位置差分，不能当作瞬时轮胎侧滑测量。

另请审查参考每0.2秒对当前估计位姿重接时，降低lookahead为何不等价于增强简单pure pursuit纠偏：重接本身也随lookahead改变有效反馈。我们已有局部解析和固定状态更新干预，仍需要闭环验证。

对外提供时应包含脚本、源字节/SHA、全部四窗和完整路线、所有候选失败、车辆参数与坐标定义；只能在确定联系人与发送范围后实际发送。Hermite已按冻结协议拒绝；固定k后续也完成并保留一项航向失败。[方向审查](pose-followup/course-audit.md)将其定位到晚core连续8帧，实际course仍有残差，不能只归因于车身/运动方向差。最初驱动启动失败无驾驶数据，独立归档，不算候选行为失败。
