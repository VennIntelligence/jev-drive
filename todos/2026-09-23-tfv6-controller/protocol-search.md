# Task 8 冻结 holdout 协议

冻结时间：2026-09-24，首次 v1 holdout 驾驶之前。候选代码为 `03f69cd`（分析代码 `6418774`）；只用 Dev10 选择。TFv6 checkpoint、LEAD、W2 的 A/B 臂及其正式结果不变。

## 固定候选与顺序

- **H 为主候选**：逐帧方向盘取 `0.5 × A + 0.5 × B`；油门/制动取 A。选择依据：Dev10 全部 10 路线 × 3 seed，均值 DS 96.94，配对 B +20.29，按路线整组 bootstrap 95% CI [5.21, 38.46]；30/30 完成。
- **J 为次候选**：方向盘取 `0.8 × A + 0.2 × B`；油门/制动取 A。Dev10 均值 DS 94.39，配对 B +17.75，95% CI [4.46, 34.92]；29/30 完成。J 在 2091/1 因官方 4000 tick 上限得 27.49 DS，故排在 H 后。
- 两候选都经原作者后处理，再交给 CARLA；裁剪方向盘到 [-1, 1]，制动时按原包装器的静止规则归一化。不得再改系数或控制代码。

## 一次性 v1 holdout 评价

`HOLDOUT_ARMS = HJ`。对 v1 holdout 的全部 6 条路线 × seed 0/1/2 各跑 H、J 一次，即 36 个有效 case；只允许 W2 规则下的基础设施重试。逐 case 立刻检查 I1 假 waypoint、I2 agent 崩溃/诊断、I3 逐帧与官方结果完整性，以及候选组合和实际执行控制一致性；发现驾驶/runner bug 立即停批。官方碰撞、偏航、超时、blocked 均是有效驾驶结果，按官方分数计。

参照 W2 正式 v1 holdout 的同 `(route, seed)` A/B 结果，不重跑 A/B。主比较为 H−B 的官方 Driving Score，次比较 H−A、J−B、J−A；同时给均值 DS、路线完成数、SR。每条路线的三个 seed 整组重抽，固定 RNG seed `20260923`、10,000 次 bootstrap，百分位 2.5/97.5 给 95% CI；不按 holdout 改候选或择优。完整披露全部 case，包括不胜 B 的情况。
