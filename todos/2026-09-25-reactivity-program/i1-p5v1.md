# I1：P5 v1，第二个 expert 与扩路线（CARLA 配对考卷加固）

状态: 预登记（2026-09-25 17:10 JST，写于生成任何 v1 world 之前；只跑过一个 200 tick 的 PDM-Lite 集成 smoke，没有看任何标签）
上级: [reactivity 计划](../2026-09-25-reactivity-program.md) 的 I1 行；前身 [P5 v0](../2026-09-24-p5-carla-pairs-v0.md)（生成器、确定性检查、标签定义、判据全部沿用）；决策第 32 条
代码: `jevdrive/p5v1.py`（路线集、索引、validity）、`jevdrive/p5_pairs.py`（v0 管线，只把 `build` 推广到多个路线源）、`scripts/p5_pair_agent.py`（recorder，新增 `driver: pdm_lite`）、`scripts/p5v1_gen.sh`、`scripts/p5v1_wait_infra.sh`、`scripts/p5v1_index.sh`
考生打分不在本文件范围内（owner 之后在 v1 上做）。

## 目标

v0 的考卷成立，但只有一个 expert（特权规则驾驶器，读仿真器真值、不看相机）：CARLA 自带的 BehaviorAgent。它刹车晚、不会在行人前提前减速，
于是两个行人 family（PedestrianCrossing 3 个 reactive 帧、VehicleTurningRoutePedestrian 10 个）没进合并，Light 只有 9 个 reactive 帧，
合并翻转率的 CI 也宽（TFv6 waypoint [28.5, 50.1]）。v1 做三件事：

1. 加第二个 expert **PDM-Lite**（carla_garage / SimLingo 作者的规则 expert，会用行人的匀速外推提前减速），两个 expert 在同一批 pair 上各开一遍、各出一套标签；BehaviorAgent 保持 v0 原样，v0 与 v1 可比。
2. base 路线从 55 条（任务书写的 25 是 v0 进合并的路线数）扩到 **101 条**，每条仍是 3 个 TM seed（Traffic Manager 的随机种子，决定背景车流），family 与 v0 相同的 10 个，null pair（只换天气、正确动作不变的对照）照 v0。
3. 让 PedestrianCrossing、VehicleTurningRoutePedestrian 和 Light 有足够的 reactive 帧 / pair 进合并数。

交付：两个 expert 的 pair 集与标签（v0 管线 `p5_pairs` 建的 pairs / 观测帧 / null 帧 / 帧索引），每个 expert 一张 label-validity 表，
以及两个 expert 在同一批 pair 上的 Δ 对比。

**与旧草稿的冲突**：[P5 v1 E 层草稿](../2026-09-24-p5-v1-e-layer.md)（2026-09-24，draft）范围更宽：把 Light 移出 E 层、加绕行类 family 和横向 Δ、加新考生。
本任务按 reactivity 计划的 I1 行执行：**Light 和两个行人 family 都留在合并集里，family 与 v0 相同，只有纵向 Δ**。草稿里其余内容不在 I1 范围。

## 设计

### 路线

「可见、需要反应」的 10 个 family 在 `bench2drive220.xml` 里每类只有 5 条（Light 10 条），v0 已经全部用上了。
v1 加上 Bench2Drive 0.0.4 自带的第二个短路线集 `bench2drive_0.0.4_val.xml`（220 条，构造方式与 220 相同：一条路线一个 scenario，约 150 m），
取其中这 10 个 family 的单 scenario 路线。两个集合的路线 id 只有 2 个重合且都不在这 10 个 family 里；按「town + 路点 + scenario 类型 + trigger 点」去重，没有重复；
v0 的 11 条已知段错误路线（server 必崩）照旧排除（它们全在 220 里，val 里没有已知崩溃表）。

| family | 220 | val | base 路线 | pair（× 3 seed） |
|:--|--:|--:|--:|--:|
| DynamicObjectCrossing | 5 | 10 | 15 | 45 |
| HardBreakRoute | 5 | 3 | 8 | 24 |
| HighwayCutIn | 5 | 4 | 9 | 27 |
| Light（红 vs 绿） | 10 | 7 | 17 | 51 |
| OppositeVehicleRunningRedLight | 5 | 1 | 6 | 18 |
| ParkingCrossingPedestrian | 5 | 2 | 7 | 21 |
| ParkingCutIn | 5 | 3 | 8 | 24 |
| PedestrianCrossing | 5 | 2 | 7 | 21 |
| StaticCutIn | 5 | 5 | 10 | 30 |
| VehicleTurningRoutePedestrian | 5 | 9 | 14 | 42 |
| **合计** | 55 | 46 | **101** | **303** |

val 的 46 条里 39 条在 Town12/13（Large Map，streamed tiles、actor dormancy，docs/carla.md 里的段错误都出在这两个 town），所以 v1 的崩溃重试会比 v0 多，每条 run 也更贵。
两个行人 family 只各加了 2 条和 9 条：PedestrianCrossing 在两个集合里一共只有 7 条。它能不能进合并，主要靠 PDM-Lite，不靠路线数。

### 世界、run 与复用

每个 case（base 路线 × seed）有 x⁺（因素在）和 x⁻（因素不在）两个世界，seed 0 另有一个 null 世界，所以每个 expert **707 个世界**（303 × 2 + 101）。
x⁺ / x⁻ / null 的构造、`_rng` 重置 hook、hazard actor 藏到地下 500 m、HardBreakRoute 把急刹换成空操作、Light 红绿互换，全部是 v0 的代码，一字未改；
`build` 对 v0 的输入重新生成的 `pairs.xml` 和 `cases.csv` 与 v0 盘上的逐字节相同（已核对）。

| expert | 树（leaderboard + scenario_runner） | 世界 | 复用 v0 | 新跑 |
|:--|:--|--:|--:|--:|
| BehaviorAgent(normal)（`ba`） | 官方 Bench2Drive 0.0.4（与 v0 相同） | 707 | 385（v0 的全部 run，逐个核对 variant XML 与 v0 相同后链接） | 322 |
| PDM-Lite（`pdm`） | SimLingo 的 Bench2Drive 副本 | 707 | 0 | 707 |
| **合计** | | 1414 | 385 | **1029** |

复用的理由：BehaviorAgent 在 v0 路线上的 run 就是 v0 那次生成，同一份 XML、同一段 recorder 代码路径（`driver` 默认 `behavior`，行为分支没动）、同一棵树。
profiling 里用 v1 代码重跑一个 v0 世界，与 v0 的位姿逐 tick 比对（见下面「profiling」），作为复用的前提检查。

### 第二个 expert：PDM-Lite

用 SimLingo 仓库自带的 Bench2Drive 副本里的 `leaderboard/team_code/autopilot.py`，原样运行（它在公开的 B2D 220 上 DS 97.0，infra 验收里 `b2d_expert_agent.py` 也是它）。
它需要那棵树在 `CarlaDataProvider.active_scenarios` 里的登记，所以 PDM-Lite 的 run 在 SimLingo 树里跑。这棵树与官方 0.0.4 的差别（[SimLingo catalogue](../2026-09-25-simlingo-catalogue/README.md) 的 D1–D7）
都不改 actor 行为：D1（4000 tick 上限）和 D2（完成阈值 90）在我们 ≤ 50 s 仿真、触发后 20 s 就截断的录制里碰不到或只影响路线结束的那一刻，D5 只记账。

接法：recorder（`p5_pair_agent.py`，本身是 TFv6 的 `SensorAgent`，挂 TFv6 自己的传感器做 shadow 推理）新增 `driver: pdm_lite`，第一个 tick 实例化一个内层 `AutoPilot`，
交给它与 BehaviorAgent 相同的稠密路线，每 tick 用同一份 `input_data` 调它的 `run_step`（它只读 IMU 的 compass，这个 id 与 TFv6 的 IMU 同名同规格，其余都直接查仿真器）。
Waymo 三相机、actor 快照、可见性、TFv6 shadow 都与 v0 一样记录，所以 owner 之后可以在 PDM-Lite 开出来的帧上考同一批考生。
三处必须处理、但不改 PDM-Lite 行为的细节：

| 细节 | 问题 | 处理 |
|:--|:--|:--|
| 转向噪声 | `control.steer += 1e-3 · np.random.randn()`，用的是进程全局的 numpy 随机流；两个世界各自一个进程、没播种，x⁺ 与 x⁻ 会从第一个 tick 起毫米级分叉，1 cm 的确定性门槛就被它触发 | 给 PDM-Lite 一条自己的随机流（每次调用前后换入 / 换出 `np.random` 的 state），以 TM seed 播种：同一 case 的 x⁺、x⁻、null 抽到同样的噪声，噪声本身保留 |
| `SAVE_PATH` | LEAD（TFv6）需要它；PDM-Lite 在 import 和 `setup` 里读到它就会开始写自己的数据集 | 只在 import / setup PDM-Lite 那一刻把它从环境里拿掉 |
| 藏起来的 hazard actor | x⁻ 把 hazard actor 放在地下 500 m；若 expert 用二维距离，就会对看不见的行人刹车 | 读代码确认：行人外推、他车 bbox 预测都先用三维距离过滤（`detection_radius` 50 m），`compute_leading_vehicles` 虽用二维距离，但只在三维过滤后的 bbox 集合上起作用；`active_scenarios` 只用于绕行类 scenario（不在我们的 10 个 family 里）。null 与 x⁺/x⁻ 在因素可见前的逐 tick 相同是经验上的检查 |

环境：PDM-Lite 的 run 用 `envs/p5v1-pdm`：一个 Python 3.10 venv，底下挂 SimLingo 的环境（`envs/simlingo`，numpy 1.23.5、torch 2.8 cu128，infra 验收里 PDM-Lite 就在这个环境里跑），
自己只装 LEAD（TFv6）import 需要而 SimLingo 环境没有的包（`--no-deps`，版本照 `envs/scout-tfv6`，numba 换成支持 numpy 1.23 的 0.60），不改动任何共享环境。
BehaviorAgent 的 run 仍用 v0 的 `envs/scout-tfv6`。（预登记时写的是「都用 `envs/scout-tfv6`、补装 `rdp`」，集成 smoke 发现 PDM-Lite 在 numpy 1.26 下崩溃后改的，见偏离记录第 1 条。）

### 标签与判据（全部沿用 v0，每个 expert 各算一遍）

- 观测帧、v2（2 s 处速度）、Δ_expert = v2(x⁺) − v2(x⁻)、reactive = |Δ_expert| > τ_exp，τ_exp = max(该 expert 的 null p95, 0.5 m/s)：v0 原文。
- label-validity 表的列与 v0 相同（`p5_exam.exam` 不带考生调用）：pair、通过确定性检查（t_div ≥ t_vis）、丢弃原因、观测帧、reactive 帧、含 reactive 的 pair、Δ 中位数、纯度、进不进合并。
- **进合并**：该 family 至少 5 个 pair 含 reactive 帧（v0 规则，按 expert 分别判）。
- **判据**（v0 原文，按 expert 分别判）：通过确定性检查的 pair < 50% → 先修生成器；合并 reactive 帧 < 100 或占合并 family 观测帧 < 20% → 标签太稀。
- **I1 的目标是否达到**（写于任何数字之前）：PedestrianCrossing、VehicleTurningRoutePedestrian、Light 三个 family 在**至少一个 expert** 下按 v0 规则进合并，算达到；
  另报每个 family 的 reactive 帧数，≥ 30 帧的 family 才说它的翻转率「有得读」（30 是本文件定的描述性门槛，不是 v0 判据）。
  「两个行人 family」指的是 v0 里没进合并的 PedestrianCrossing 和 VehicleTurningRoutePedestrian；reactivity 计划里写的「45 + 76」是 DynamicObjectCrossing 和 ParkingCrossingPedestrian 的 reactive 帧（v0 已进合并），两处说法不一致，以没进合并的两个为目标，四个行人类都报。

### 两个 expert 在同一批 pair 上的对比（预登记）

两个 expert 开出来的 ego 轨迹不同，观测帧也就不同，所以对比按 pair 做，不按帧配对。只取两个 expert 下都通过确定性检查的 pair，按 family 报：
含 reactive 帧的 pair 数（BA / PDM / 两者 / 只有一方）、reactive 帧数、**反应提前量**（第一个 reactive 观测帧减 t_vis，秒）的中位数、reactive 帧上 Δ 的中位数、每 pair 最小 Δ 的中位数。

读法：PDM-Lite 在两个行人 family 上含 reactive 的 pair 明显多于 BehaviorAgent、提前量更小 → v0 的「行人没题」是 expert 造成的，v1 的行人题来自 PDM-Lite；
两个 expert 都 < 5 → 这两个 scenario 的行人在前视画面里出现时，任何一个规则 expert 都还不需要反应（scenario 设计问题），不是 expert 的问题；
两个 expert 在 cut-in family 上一致（含 reactive 的 pair 数相近）是 sanity check。

## 计划的数字（生成之前）

| 项 | 量 |
|:--|:--|
| base 路线 / pair / null | 101 / 303 / 101 |
| 每个 expert 的世界 | 707 |
| 新跑的 run | 1029（BA 322 + PDM 707） |
| 每 run | v0 实测中位 276 s（与训练任务共享 GPU）；v1 的 val 路线大多在 Town12，预计更贵，由下面的 profiling 定 |
| 盘 | v0 每 run 约 70 MB → 约 72 GB |
| 资源 | GPU 4，I3 结束后加 GPU 2（原计划夜间 GPU 1、2，见偏离记录第 2 条）；CPU 按 layout 约 64 核，NUMA node 1；CARLA server index 800–899 |

粗估（profiling 前）：1029 × 4.6 min / 8 实例 ≈ 9.9 h。超过 8–10 h 预算的 2 倍（20 h）就不启动，先报。

## 布局与启动

原计划是 21:30 之后、等 infra agent 的 CARLA profiling 结束、再等 owner 确认 layout 最多 30 min 才启动（夜间，GPU 1、2）。
**16:20 CST main 改了排程**（见偏离记录第 2 条）：infra agent 在 gpu-plan.md 写出 `[INFRA-PROFILE] layout ready` 就启动（目标约 18:00），
用它报的 layout，先在 GPU 4 上跑，GPU 2 等 I3 的渲染结束后再加上；不再等 owner 确认。现在挂的是：

| slot | 门 | 做什么 |
|:--|:--|:--|
| `p5v1-wait-infra` | 无 | `scripts/p5v1_wait_infra.sh`：每分钟看一次 gpu-plan.md，出现含 `[INFRA-PROFILE] layout ready` 的行就退出；行里若有 `servers_per_gpu=N`、`cores_per_server=C`，写进 `runs/p5v1/layout.infra.env` |
| `p5v1-gen` | `--after p5v1-wait-infra` | `scripts/p5v1_gen.sh`：读 `runs/p5v1/layout.env`（我们的默认值，见 profiling 一节），再读 `layout.infra.env` 覆盖每卡实例数和每实例核数；每张卡一条链 |
| `p5v1-index` | `--after p5v1-gen` | `scripts/p5v1_index.sh`：两个 expert 各跑一遍 v0 管线（`processed/carla_p5v1_{ba,pdm}`），再出 validity 与对比表（`research/results/p5-v1/`） |

链的细节：GPU 4 的链立刻开始；GPU 2 的链（`GATE_2=i3`）先等 I3 在这张卡上的渲染结束：`layout.env` 里写了 `I3_FINAL_SLOT` 就等那个 sentinel，
否则等「至少一个 `i3-*.done`、没有任何 `jev:i3-*` 窗口还有活的子进程、卡上空闲 ≥ 40 GB」连续 3 次（间隔 5 min）成立。
每条链从自己的 CPU 列表（GPU 4：52–103，GPU 2：156–189，都在这两张卡的 NUMA node 1 上）里取「实例数 × 每实例核数」个核，
先剔除启动那一刻任何活进程被 pin 到的核，所以不会和别的任务或另一条链重叠。每条链按 pass（2 遍）× expert（先 PDM-Lite 再 BehaviorAgent）
跑该 expert 还没完成的世界；所有链共用每个 expert 一个 `--out`（claim 分路线），第 j 条链占 server index 800 + 50j 起的 50 个（`b2d_run --index-span`，挪 server 时在块内回绕）。
PDM-Lite 用 SimLingo 的树与 `envs/p5v1-pdm`，BehaviorAgent 用官方树与 v0 的 `envs/scout-tfv6`。用了哪个 layout 复制成 `layout.used-<时间>.env`，每条链的开始 / 结束写进 gpu-plan.md（`[REACTIVITY/I1]`）。
每个 expert 完成 ≥ 95% 的世界就以 0 退出，index slot 在已有的世界上建。

**实际启动**：`[INFRA-PROFILE] layout ready` 那一行 16:20 就已经写了（没有机器可读的 key，用 layout.env 的默认值），`p5v1-wait-infra` 挂上即通过。
16:48 第一次启动后我立刻停了它（kill 自己的 tmux 窗口，残留的 2 个 CARLA server 和 1 个 route 进程按 PID 组清掉，一个世界都没跑完），
补上 `--client-threads 8` 和线程上限保护（infra agent 16:46 的警告：容器 pids.max 20480，当时已用约 17.9k）后，16:53 重新启动：
GPU 4 链 6 个实例（CPU 97–103、156–169、190–195、202–204），GPU 2 链 16:54 也启动了，因为 `i3-final.done` 16:50 已经出现（CPU 170–189、104–113）。
GPU 4 上 infra agent 的 profiling（`prof-ladder3`，CARLA index 747–749）当时仍在同卡跑（已告知 main），卡上显存到了 94.6 / 97.9 GB，
GPU 4 的一个 server 以 rc 139 崩了一次；GPU 2 的前两个 server 撞上了 ephemeral 端口（`bind: Address already in use`）。
所以 16:59 我按 PID 停了这一轮（SIGINT 让两个 runner 取消在跑的 route，残留的 2 个 server 按进程组清掉；已完成的 4 个 PDM-Lite 世界保留），
给 `b2d_run` 加上带 `--index-span` 时的端口 bind 预检（被占的 slot 直接跳过，只作用于我们的 runner），GPU 4 改为 5 个实例，
**17:01 第三次启动**：GPU 4 链 5 个实例（CPU 97–103、156–169、190–193），GPU 2 链 6 个（CPU 170–189、104–113）。
17:02 infra agent 把剩下的 ladder 挪去了 GPU 0，GPU 4 现在只有我们。`p5v1-index` 一直在等 `p5v1-gen`，没有重挂。

续跑：被杀后重新挂同样的三个 slot（`p5v1-wait-infra` 会因为那一行已经在 gpu-plan.md 里而立即通过），已完成的世界跳过，死掉的 runner 的 claim 按 pid 立刻被接管，
上一轮留下的 CARLA server 由启动时的 reap 清掉。

## profiling（2 对端到端，2026-09-25 16:19–16:50 CST）

`scripts/p5v1_profile.sh a 3`（run dir `runs/p5v1/prof-a`）：pair A = 27515 seed 0（PedestrianCrossing，Town03，v0 路线）的 x⁺ / x⁻ / null，
pair B = 11177 seed 0（VehicleTurningRoutePedestrian，Town12，v1 新路线）的 x⁺ / x⁻；先 PDM-Lite 5 个世界、再 BehaviorAgent 5 个世界，
每次 2 个 CARLA 实例并行，GPU 2（与别的任务共卡，后半段 I3 的渲染也上来了），CPU 190–195 + 202–207，`scripts/p5v1_prof_sampler.py` 每 5 s 采样；
然后走 v0 管线（`p5_pairs.collect`）到观测帧和标签。PDM-Lite 用的是 v0 的录制窗口（偏离记录第 3 条是看完这一步之后定的）。

**每个 run**（agent 的逐 tick 分项来自 `p5_summary.json`，ms 为均值）：

| 世界 | expert | tick 数 | wall (s) | 每 tick | TFv6 相机 tick（其中 forward） | TFv6 其余 tick | expert | 存图（相机 tick） |
|:--|:--|--:|--:|--:|--:|--:|--:|--:|
| 27515 x⁺ / x⁻ / null（Town03） | PDM-Lite | 453 | 152–185 | 215–313 | 265–302（185–223） | 48–56 | 18–74 | 36–39 |
| 11177 x⁺ / x⁻（Town12） | PDM-Lite | 433 | 310–378 | 437–523 | 367–444（258–318） | 64–67 | 39–42 | 35–38 |
| 27515 x⁺ / x⁻ / null | BehaviorAgent | 473 | 245–281 | 342–439 | 471–485（329–347） | 53–73 | ≈ 1 | 44–52 |
| 11177 x⁺ / x⁻ | BehaviorAgent | 441 | 460–463 | 699–710 | 559–570（373–395） | 83–93 | 13–14 | 59 |

BehaviorAgent 那一半跑在后面，GPU 2 上 I3 的渲染已经起来了（卡上显存从 20 GB 升到 52–81 GB），所以它的每 tick 比 PDM-Lite 慢，不是 expert 本身贵。
route setup（load world + scenario 构建）约 40–60 s，Town12 更长。

**每个 CARLA 实例**（sampler，有 route 在跑的 507 个采样）：CPU 中位 3.0 核、p95 4.2 核（server 1.9 / route client 1.0）；RSS 中位 8.9 GB、p95 11.7 GB；
VRAM 每实例约 9–10.5 GB（CARLA 6.9–8.1 GB + TFv6 三 seed ensemble 2.25 GB，`nvidia-smi --query-compute-apps`）。线程数：CARLA server 约 430，
route client 默认约 215（`carla.Client` 按宿主机 208 个硬件线程开 worker），批量里用 `--client-threads 8` 压下来（infra agent 16:46 的线程上限警告）。

**瓶颈**：每 tick 的大头是 TFv6 shadow（第三方，照原样跑）：相机 tick 上 265–570 ms，其中 ensemble forward 185–395 ms，其余 tick 的 `BaseAgent.tick` 50–90 ms，
加起来约占 tick 的 40–50%；其次是 CARLA 的 world tick 和传感器等待（渲染 3 台 Waymo 相机 + TFv6 的 3 相机、2 LiDAR、4 radar + 分割相机），再次是 expert（PDM-Lite 18–74 ms，BehaviorAgent ≈ 1 ms）。
**我们自己的代码**只占约 3–4%：

| 我们的分项 | 优化前 | 优化后 | 等价性 |
|:--|:--|:--|:--|
| 三相机 remap + JPEG（每相机 tick） | 42.9 ms（串行；v0 实测 65–75 ms） | 30.8 ms（`save_threads: 3`，cv2 释放 GIL） | 40 帧真实渲染上，串行与并行写出的 JPEG 字节逐个相同（md5） |
| 可见性（分割图投影） | 3–6 ms，但**每个 run 第一个相机 tick 白等 5 s**（分割相机在这个 tick 才生成，这一帧不可能有图；v0 也是等满 5 s 后记 None） | 第一个相机 tick 直接记 None | 输出相同（v0 的 frames.jsonl 第一帧 px 都是 null）；每 run 省 5 s，1029 个 run 约 1.4 实例小时 |
| actor 快照 | 0.2–3 ms | 不动 | — |

**确定性与复现**：PDM-Lite 下两对的 x⁺、x⁻（以及 27515 的 null）**整个录制逐 tick 相同**（1 cm / 0.1° 门槛从未触发），说明给 PDM-Lite 单独的随机流有效、藏在地下的 hazard actor 不影响它。
BehaviorAgent 用 v1 代码重跑 v0 的两个世界（2751510、2751520）：473 个 tick 的位姿与 v0 **完全相同**（最大 0.000000 m），119 个相机帧的可见像素也逐帧相同，复用 385 个 v0 run 的前提成立。

**标签**：这两对在两个 expert 下都是 0 个 reactive 帧——ego 在触发后很快停在红灯前，录制在灯变绿之前就结束了（见偏离记录第 3 条）。

**批量估计**（写于批量开始前）：PDM-Lite 加长窗口后，一个 run 平均约 900 tick；每实例在一卡 6 个时约 0.45 s/tick（infra agent 的 Alpamayo rig 在 N = 6 时每 worker 比 N = 1 慢 1.36×），
加 setup 约 7–8 min / run；BehaviorAgent 的新 run 大多在 Town12，约 6 min / run。合计约 707 × 7.8 + 322 × 6 ≈ 7400 实例分钟，
12 个实例（两张卡各 6 个）约 **10 h**；GPU 4 上 infra agent 的 profiling 还在同卡跑（3 个 server），实际可能到 12–14 h。在 8–10 h 预算的 2 倍（20 h）以内，所以启动了。

**layout.env 默认值**（`runs/p5v1/layout.env`；infra 那一行没有机器可读的 key，所以用的是这里的值）：`GPUS="4 2"`、`WORKERS="6 6"`（infra 的 knee：每卡 6 个）、
`CORES_PER_SERVER=5`（实测 p95 4.2）、`VRAM_GB_PER_SERVER=11`、`CPUS_4="97-103,156-169,190-195,202-207"`、`CPUS_2="170-189,104-155"`（都先剔除启动时被 pin 的核）、
`GATE_2=i3`、`I3_FINAL_SLOT=i3-final`、`EXPERTS="pdm ba"`；线程上限按每实例 600 个线程、总预算 19000 限制实例数。

## 偏离记录

（任何偏离先写在这里，写于看到受影响的数字之前。）

1. **PDM-Lite 的运行环境**（2026-09-25 16:15 CST，集成 smoke 之后、profiling 之前，没有任何标签）。预登记写的是 PDM-Lite 与 BehaviorAgent 都在 `envs/scout-tfv6`（numpy 1.26）里跑。
   smoke 里 PDM-Lite 的第一个 tick 就崩了两次：先是 `np.float`（numpy 1.24 删掉的别名），再是 `kinematic_bicycle_model.forecast_ego_vehicle` 里 `np.array([speed_kph, …])`
   把 shape (1,) 的数组和标量混在一起——numpy 1.23 下这是一个带警告的 object 数组、照样算得下去，1.24 起直接报错。PDM-Lite 的数值路径依赖旧 numpy 的语义，
   所以改为在它作者环境的 numpy（SimLingo 环境的 1.23.5）里跑：新建 `envs/p5v1-pdm`（见上面「环境」）。别名恢复（`np.float = float` 等）留在 recorder 里，对 1.23 是空操作。
   影响：PDM-Lite 那一半 run 里的 TFv6 shadow 用的是另一组包版本（numba 0.60 对 0.61、scipy 1.10 对 1.14 等）；TFv6 的 Δ 是同一 expert 的 x⁺ 对 x⁻，两侧同一环境，不受影响；
   跨 expert 比较 TFv6 的绝对输出时要记得这一条。BehaviorAgent 那一半不变。
2. **排程与资源**（2026-09-25 16:20 CST，main 转来的排程变更，任何生成之前）。原来：21:30 之后、infra profiling 结束、owner 确认 layout（最多等 30 min）才启动，GPU 1、2，CPU 100–149 + 196–207。
   现在：gpu-plan.md 出现 `[INFRA-PROFILE] layout ready` 就启动（目标约 18:00），用那一行 / infra 文档的 layout，GPU 4，I3 结束后加 GPU 2，不等 owner 确认；
   profiling 的 CPU 从 196–207 改成 190–195 + 202–207（196–201 归 owner）。只影响何时、在哪跑，不影响设计。
3. **PDM-Lite 的录制窗口加长**（2026-09-25 16:40 CST，profiling 之后、批量之前）。v0 的 recorder 在触发后 20 s、静止 30 s 或仿真 50 s 时停。
   profiling 的两对（27515 PedestrianCrossing、11177 VehicleTurningRoutePedestrian，seed 0）在 PDM-Lite 下都是：scenario 在开头 1.7–2.7 s 就触发，
   ego 在 t ≈ 4 s 停在一盏红灯前（停止线前 12 m / 路口处，逐帧查过灯态），一直到录制结束灯还是红的；两对的 x⁺、x⁻、null 从头到尾逐 tick 相同，没有 reactive 帧。
   也就是说录制在 scenario 真正演到 ego 面前之前就截断了；v0 smoke 在 27515 上看到过同一件事（那次把触发后的时长定成了 20 s）。
   改为：**PDM-Lite 的 run 录到触发后 40 s、静止 40 s 或仿真 70 s**（`agent-pdm.json`，由 `scripts/p5v1_gen.sh` 写出）；BehaviorAgent 的新 run 保持 v0 的 20 / 30 / 50，
   与复用的 385 个 v0 run 一致。代价：等红灯的 run 更长，PDM-Lite 那一半的平均 run 时长估计从约 4 min 升到约 6–8 min（见 profiling 一节的估计）。
   为了让两个 expert 的比较不被窗口长短混淆，**PDM-Lite 的标签报两套**：全窗口（进不进合并按这一套判，这是本条改动的目的），
   和「截回 v0 窗口」——只保留 5 s 未来落在按 v0 规则两个世界都还在录的范围内的观测帧（`p5v1.v0_window`：用每个世界自己的位姿和触发时刻重放 v0 的三条停止规则）。
   两个 expert 的逐 pair 对比以截回 v0 窗口那一套为主，全窗口那一套并列报。
   这条改动是在看到这两对 profiling pair 的结果（0 个 reactive 帧）之后定的，但它针对的是录制截断，不是任何标签的值；其余 99 条路线的标签都还没有生成。

## 结果

（跑完再填。）
