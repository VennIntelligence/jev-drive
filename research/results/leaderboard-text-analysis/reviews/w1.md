# W1 消融账本独立复核

- 原始账本：`/data/hack_audit/out2/w1_ablation_ledger.csv`，1,121 条数据行；复核快照 SHA-256 `09f8b6e981244194844641435d9ff25f7f03a1dda06c6b753f89ddb595498293`（2026-09-24 22:18:58 JST）。本报告中的行号是 **CSV 物理行号，表头为第 1 行**。
- 抽样：`seed=20260924`，执行 `sorted(random.Random(seed).sample(range(n), max(10, ceil(0.1*n))))`，`n=1121`，抽中 **113 行**。每条按 `source` 指定的本地 PDF 页、表或图核对方法/配置、原始两端分数、`delta`、指标、划分、component、权重/训练方式、可核的运行数和样本量及首轮 finding ID。图表读的是原 PDF（图 4 两项另看渲染图），不是仅验算差值。
- 结论：**106/113 通过，7/113 需修正，一致率 93.81%，达到 README 的 90% 门槛。** 110 条原账本两端均有数字，显示数字的差值算术均正确；3 条 `N/A` 中第 84 行有合理原因，第 228、433 行误将图中直接标注的数字视为无法读取。

## 全部抽样行与判定

| 方法 | PDF | 抽中 CSV 物理行号（`!` 为不通过） |
| --- | --- | --- |
| TFv6 | `tfv6.pdf` | 2, 24 |
| AD-MLP | `ad_mlp.pdf` | 29, 33 |
| SimLingo-BASE | `simlingo_base.pdf` | 44, 46, 48, 666 |
| CarLLaVA | `carllava.pdf` | 51 |
| TF++ | `tfpp.pdf` | 56, 75, 81 |
| LTF/TransFuser | `ltf.pdf` | 84 |
| SparseDriveV2 | `sparsedrivev2.pdf` | 118, 119, 121, 125 |
| SparseOccVLA | `sparseoccvla.pdf` | 151, 156, 164, 179, 184, 198, 214, 218, 223, 225, **228!** |
| DriveMA-4B | `drivema.pdf` | 243 |
| NTR | `ntr.pdf` | 263, 274, 284 |
| DrivoR | `drivor.pdf` | 299, 311 |
| DriveVLA-M0 | `drivevla_m0.pdf` | 343, 355 |
| DrivoR+TOAD | `toad.pdf` | 389, 390 |
| TOAD | `toad.pdf` | 424, **433!** |
| AutoVLA | `autovla.pdf` | 446, 449, 451, 452, 458 |
| WA-JEPA | `wa_jepa.pdf` | 493, 502 |
| UniAD | `uniad.pdf` | 510, 524, 533, 542, 546 |
| BLUE | `blue.pdf` | **562!**, **569!**, **571!**, 594, 598, **599!** |
| BEV-Planner++ | `bev_planner.pdf` | **608!**, 621, 630, 637, 646 |
| Senna | `senna.pdf` | 684 |
| LinkVLA | `linkvla.pdf` | 708, 712, 718, 719 |
| FIVE-VLA | `five_vla.pdf` | 755, 758, 762, 766, 773, 775, 779, 805, 820, 837, 846, 851, 863, 875, 887, 888, 890, 906, 909, 915, 927, 929, 935, 942, 951, 965 |
| Kyber-E2E | `kyber_e2e.pdf` | 995, 1000, 1001, 1002, 1007 |
| DriveFuture | `drivefuture.pdf` | 1013, 1029, 1030, 1032, 1033 |
| OmniSpace | `omnispace.pdf` | 1039, 1041, 1055, 1059, 1082 |
| LVLDrive | `lvldrive.pdf` | 1088, 1105, 1108 |
| Poutine | `poutine.pdf` | 1121 |

表中共 113 个行号，其中粗体 `!` 的 7 行为不通过，其余 106 行通过。每个样本的完整来源定位和记录字段见账本原行；本复核使用本地 `/data/hack_audit/papers/` 下同名 PDF。

## 需修正的 7 行

| 行号 | 核验原文与问题 | 建议纠正 |
| ---: | --- | --- |
| **228** | `sparseoccvla.pdf` PDF p.7 Figure 4(a) 的蓝色 CIDEr 曲线在 300 与 750 queries 旁分别**直接标注 0.732 与 0.778**；正文也明确 300 queries 的 CIDEr 为 0.732。账本把两端及 `delta` 记为 `N/A`，其 notes 所说“无法可靠配对”与图不符。 | `baseline_config=300 queries`，`variant_config=750 queries`，`baseline_score=0.732`，`variant_score=0.778`，`delta=+0.046`；删除无法读数的理由。 |
| **433** | `toad.pdf` PDF p.8 Figure 4(b) 是 warmup-two-stage 的候选数 `M` 曲线；横轴最小 `M=2`、最大 `M=64`，点旁**直接标注 38.5 与 48.5 EPDMS**。账本将其写为 `N/A`，notes 所称无法配对不成立。论文 PDF p.5 还说明该 ablation 用 7 个场景，账本的 `n_samples` 这一项正确。 | 把配置写明 `M=2` → `M=64`，填 `38.5` → `48.5`、`delta=+10.0`；删除无法读数的理由。 |
| **562** | `blue.pdf` PDF p.9 Table 10 的 Fail2Drive Generalization DS `71.7` → `73.9` 正确；但 BLUE 用冻结 SimLingo backbone 的隐状态**另行训练 0.11M 参数 gate**。账本标 `same_weights`，与 W1 codebook 将该值限定为固定基础 checkpoint、只改推理输入/规则/搜索的定义冲突。 | `same_weights_or_retrained` 改为 `retrained`（仅 gate 训练；backbone 冻结），并在 notes 明示。 |
| **569** | `blue.pdf` PDF p.6 Table 6 的 SimLingo `67.27` → SimLingo + matched gate `76.18` SR 正确；正文说每个模型单独训练自己的 gate。`same_weights` 未表达新增的已训练 gate。 | 改为 `retrained`（gate-only；SimLingo backbone 冻结），或在字段中明确独立训练 gate。 |
| **571** | 同一 Table 6 的 CriticVLA `73.33` → CriticVLA + **SimLingo-trained transferred gate** `73.11` SR 正确。该变体组合了 CriticVLA 与另一模型上训练的 gate；`same_weights` 把它误写成纯推理规则变化。 | 标为 `mixed_checkpoints`（固定 CriticVLA backbone + 外来训练 gate），并在 notes 说明。 |
| **599** | `blue.pdf` PDF p.9 Table 11 的 ReCogDrive `84.13` → BLUE gate `87.00` PDMS 正确；同页正文明确 gate 在 NAVSIM 训练集的 90% 上训练、其余 10% 选阈值。`same_weights` 忽略 gate 参数学习。 | 改为 `retrained`（仅 gate 训练；ReCogDrive backbone 冻结），在 notes 保留 90%/10% 设定。 |
| **608** | `bev_planner.pdf` PDF p.5 Table 1 将 UniAD ID2（ego in BEV only，collision Avg. `0.62`）的 checkpoint source 标成 **Official**，ID3（BEV + planner，`0.37`）标成 **Reproduce**；表注称作者对 ID3 做了代码改动。账本的数值、`delta=-0.25` 正确，但将 `component=ego_state_planner` 的这对比较写成没有任何限定的单成分增益，漏掉 checkpoint/实现来源混杂。 | 在 notes 明确“ID2 官方 checkpoint vs ID3 作者复现，不能把差值单独归因于 planner ego state”；可保留该比较作带混杂的对照。 |

## 其他核验说明

- 首轮 finding ID：抽到的 `REAL-NUS-001`（29）、`REAL-NUS-006`（33）、`LB2-TFPP-001`（81）、`NAV2-TOAD-001`（389）、`REAL-NUS-005`（608、621）与对应机制一致。第 243 行的 `REAL-WOD-001` 指向把官方 RFS 用作 `Rtraj` 奖励，而它比较的是 `Rtraj` → `Rtraj+Rcons`，两端已有 RFS 奖励；ID 可作背景关联，但**不能解释该行的增量**，建议在 notes 加一句限定。此项没有计入不通过。
- 第 84 行 `ltf.pdf` PDF p.8 Table 2 把默认 TransFuser 分成 3 个种子（PDMS 83.3/84.0/84.4），`Goal only` 为 81.8；账本没有随意选一个种子作配对基线，因此 `N/A` 合理。
- 分数在原表中常带尾零（如 `0.300`/`0.3`、`90.40`/`90.4`）；只在数值相等时视为一致。第 1121 行的 7.91 基线由 `poutine.pdf` PDF p.4 Results 明确给出，p.1 Figure 1 提供排序图，未把验证集的 8.12 混入 test 比较。
- 本报告仅写入 `out2/reviews/w1.md`；未修改主账本。
